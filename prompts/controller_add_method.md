# Add method to controller

Add a new method HTTP_METHOD on path PATH to existing controller CONTROLLER.
Create any necessary business layer handlers, or use existing (controllers usually already have dedicated business layer handlers).
Method should return DESCRIPTION.