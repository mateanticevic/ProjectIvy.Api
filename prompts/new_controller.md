# New controller with GET method

Create a new controller (see existing BankController as reference) for resource {NAME_OF_THE_RESOURCE} with a GET method that provides search functionality and returns paged results. Create any necessary business layer handlers and models.